meta {
  name: 更新视图
  type: http
  seq: 4
}

put {
  url: {{base_url}}/api/views/{{view_id}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "name": "更新后的视图"
  }
}

settings {
  encodeUrl: true
}
