meta {
  name: 删除视图
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/views/{{view_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
}
