meta {
  name: 视图列表
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/views?table_id={{table_id}}
  body: none
  auth: inherit
}

params:query {
  table_id: {{table_id}}
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
}
