meta {
  name: 更新基础表
  type: http
  seq: 4
}

put {
  url: {{base_url}}/api/bases/{{base_id}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "name": "更新后的基础表"
  }
}

settings {
  encodeUrl: true
}
