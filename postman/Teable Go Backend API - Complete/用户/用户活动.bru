meta {
  name: 用户活动
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/users/{{user_id}}/activity?limit=10&offset=0
  body: none
  auth: inherit
}

params:query {
  limit: 10
  offset: 0
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
}
