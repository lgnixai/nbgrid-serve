meta {
  name: 删除记录
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/records/{{record_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
}
