meta {
  name: 更新字段
  type: http
  seq: 4
}

put {
  url: {{base_url}}/api/fields/{{field_id}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "name": "更新后的字段名"
  }
}

settings {
  encodeUrl: true
}
