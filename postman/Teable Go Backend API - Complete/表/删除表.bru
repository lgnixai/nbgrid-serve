meta {
  name: 删除表
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/tables/{{table_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
}
